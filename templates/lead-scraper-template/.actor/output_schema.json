{
    "actorOutputSchemaVersion": 1,
    "title": "{{ACTOR_TITLE}} Output",
    "properties": {
        "dataset": {
            "type": "string",
            "title": "{{ACTOR_TITLE}} dataset",
            "template": "{{datasetUrl}}"
        }
    }
}
