{
  "actorOutputSchemaVersion": 1,
  "title": "Global Xero & QuickBooks Accounting Firm Leads Output",
  "properties": {
    "dataset": {
      "type": "string",
      "title": "Lead dataset",
      "description": "Deduplicated accounting firms across requested locations with Xero and QuickBooks source provenance.",
      "template": "{{datasetUrl}}"
    },
    "summary": {
      "type": "string",
      "title": "Run summary",
      "description": "Normalized effective locations plus directory search, profile, result, merge-reason, and source-failure counters.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
    }
  }
}
