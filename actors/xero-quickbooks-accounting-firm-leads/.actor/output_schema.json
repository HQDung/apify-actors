{
  "actorOutputSchemaVersion": 1,
  "title": "Global Xero & QuickBooks Accounting Firm Leads Output",
  "properties": {
    "dataset": {
      "type": "string",
      "title": "Lead dataset",
      "description": "Deduplicated accounting firms across requested locations with Xero and QuickBooks source provenance.",
      "template": "{{datasetUrl}}"
    },
    "summary": {
      "type": "string",
      "title": "Run summary",
      "description": "Normalized effective locations plus directory search, profile, partial-profile, result, merge-reason, source-failure, retry-attempt, and rendered-pagination counters. Build 0.1.6 completed the 15-case global cloud gate.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
    }
  }
}
