{
  "actorOutputSchemaVersion": 1,
  "title": "Global Xero & QuickBooks Accounting Firm Leads Output",
  "properties": {
    "dataset": {
      "type": "string",
      "title": "Lead dataset",
      "description": "Deduplicated accounting firms across requested locations with Xero and QuickBooks source provenance.",
      "template": "{{datasetUrl}}"
    },
    "summary": {
      "type": "string",
      "title": "Run summary",
      "description": "Runtime-aligned effective input plus directory search, profile, partial-profile, result, merge-reason, per-source failure, retry-attempt, rendered-pagination, and optional website attempts/successes/failures/page/contact/service/industry coverage and domain-timeout counters. Website enrichment is bounded per canonical domain and disabled by default.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
    }
  }
}
