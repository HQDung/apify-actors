{
  "title": "London Xero & QuickBooks Accounting Firm Leads",
  "description": "Find public accounting firm and advisor profiles in London, United Kingdom, from Xero and QuickBooks directories. Website enrichment is not available.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "sources": {
      "title": "Sources",
      "type": "array",
      "description": "Advisor directories to search.",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["xero", "quickbooks"],
        "enumTitles": [
          "Xero Advisor Directory",
          "QuickBooks ProAdvisor Directory"
        ]
      },
      "default": ["xero", "quickbooks"],
      "uniqueItems": true
    },
    "maxResults": {
      "title": "Maximum leads",
      "type": "integer",
      "description": "Maximum number of final deduplicated London leads. When both Xero and QuickBooks are selected, values below 14 are normalized to a combined-source floor of 14; single-source runs are unchanged.",
      "default": 14,
      "minimum": 1,
      "maximum": 5000
    },
    "extractContacts": {
      "title": "Extract public contact people",
      "type": "boolean",
      "description": "Extract publicly listed accounting-firm contacts.",
      "default": false
    },
    "includeRawData": {
      "title": "Include raw source data",
      "type": "boolean",
      "description": "Include compact source fields for debugging.",
      "default": false
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Proxy settings for public directory requests.",
      "editor": "proxy",
      "default": { "useApifyProxy": true }
    }
  }
}
