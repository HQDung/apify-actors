{
  "title": "Global Xero & QuickBooks Accounting Firm Leads",
  "description": "Find public accounting firm and advisor profiles worldwide from Xero and QuickBooks directories. Build 0.1.6 validated UK, US, Australia, and Singapore directory routes in the global cloud gate; runs expose bounded directory and optional website-enrichment counters in OUTPUT.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "locations": {
      "title": "Locations",
      "type": "array",
      "description": "Cities, regions, postcodes, or countries to search. UK, US, Australia, and Singapore routes are validated first; other locations are best-effort.",
      "editor": "stringList",
      "items": { "type": "string" },
      "minItems": 1,
      "maxItems": 20,
      "default": ["London, United Kingdom"]
    },
    "sources": {
      "title": "Sources",
      "type": "array",
      "description": "Advisor directories to search.",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["xero", "quickbooks"],
        "enumTitles": [
          "Xero Advisor Directory",
          "QuickBooks ProAdvisor Directory"
        ]
      },
      "default": ["xero", "quickbooks"],
      "uniqueItems": true
    },
    "maxResults": {
      "title": "Maximum leads",
      "type": "integer",
      "description": "Maximum number of final deduplicated leads across all locations and sources.",
      "default": 100,
      "minimum": 1,
      "maximum": 5000
    },
    "extractContacts": {
      "title": "Extract public contact people",
      "type": "boolean",
      "description": "Extract publicly listed accounting-firm contacts.",
      "default": false
    },
    "enrichWebsites": {
      "title": "Enrich company websites",
      "type": "boolean",
      "description": "Opt-in bounded website enrichment. Each canonical public domain gets at most three HTML pages, a 10-second page deadline, a 30-second domain budget, two retries, abortable requests, and low concurrency; failures preserve the directory record. Default is false.",
      "default": false
    },
    "includeRawData": {
      "title": "Include raw source data",
      "type": "boolean",
      "description": "Include compact source fields for debugging.",
      "default": false
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Proxy settings for public directory requests.",
      "editor": "proxy",
      "default": { "useApifyProxy": false }
    }
  },
  "required": ["locations"]
}
