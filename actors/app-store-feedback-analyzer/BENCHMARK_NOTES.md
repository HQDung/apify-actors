# Benchmark notes

## Phase 0B — Apple source prerequisite

The initial local benchmark uses deterministic fixtures for English and Vietnamese normalized contracts, paginated feed parsing, deduplication, partial HTTP failure, shared-core attachment, multi-app aggregation, and release-impact input validation.

Cloud runtime, public feed coverage, and analysis cost remain unmeasured until an authorized cloud smoke run is available. No pricing or publication action is performed automatically.
